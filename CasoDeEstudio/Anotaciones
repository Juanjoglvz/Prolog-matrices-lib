comandos:
  Compilacion: swipl-ld -c file.c //Usa el asistente de prolog para incluir SWI-Prolog.h automaticamente (Menos mal)
  Enlazar: swipl-ld -shared -o file.so file.o //Lo mismo. El archivo .so es el que se incluira en prolog (solo en linux)
  Icluir en prolog: load_foreign_library(file) // file.so pero sin extension
  
Cosas para recordar:
  Los tipos de retorno tienen que ser tipos de prolog (Por eso se usa PL_unify_atom_chars(u, "HelloWorld!");), porque un char* no es un tipo de prolog y casca.
  Los nombre de funciones tienen que empezar en minuscula (porque al hacer load_foreign_library(Library) trata Library como variable)
  Hay que "installar la funcion" al final usando PL_register_foreign(obj, arity, function, flags); http://www.swi-prolog.org/pldoc/man?CAPI=PL_register_foreign_in_module
  
  
